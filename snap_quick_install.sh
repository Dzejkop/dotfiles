#!/usr/bin/env bash

sudo snap install code --stable --classic
sudo snap install spotify --stable
sudo snap install slack --stable --classic
sudo snap install nvim --beta --classic
sudo snap install node --edge --classic
sudo snap install htop --stable
